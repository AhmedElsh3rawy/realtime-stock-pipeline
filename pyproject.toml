[project]
name = "realtime-stock-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka[avro]>=2.13.0",
    "fastavro>=1.12.1",
    "psycopg2-binary>=2.9.11",
    "python-dotenv>=1.2.2",
]
